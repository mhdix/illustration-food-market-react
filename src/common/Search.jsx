import React from 'react'
import { useState } from 'react';
import * as data from '../data.js'
import { useCart } from '../provider/ProductProvider';
import { useCartAction } from './../provider/ProductProvider';
const Search = () => {
return(null)
}

export default Search